/* ACTUAL MAP PREVIEW — replace the decorative map-pattern rules */
.map-card-large{
  position:relative;
  min-height:360px;
  border-radius:24px;
  overflow:hidden;
  background:#d7ebe4;
  border:1px solid rgba(7,59,60,.08);
  box-shadow:0 22px 60px rgba(5,51,51,.08);
}

.map-card-large .serenity-map-iframe{
  position:absolute;
  inset:0;
  width:100%;
  height:100%;
  min-height:360px;
  border:0;
  display:block;
  filter:saturate(.88) contrast(.98);
}

.map-card-large::after{
  content:"";
  position:absolute;
  inset:0;
  pointer-events:none;
  background:linear-gradient(90deg,rgba(3,62,62,.06),transparent 45%,rgba(255,255,255,.04));
}

.map-card-copy{
  z-index:2;
}

@media (max-width:640px){
  .map-card-large,
  .map-card-large .serenity-map-iframe{
    min-height:300px;
  }

  .map-card-copy{
    left:16px;
    top:16px;
    right:16px;
    bottom:auto;
    max-width:none;
  }
}
